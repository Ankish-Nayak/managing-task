import { ClickedDirective } from './clicked.directive';

describe('ClickedDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickedDirective();
    expect(directive).toBeTruthy();
  });
});
