import { ClickedEnterDirective } from './clicked-enter.directive';

describe('ClickedEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickedEnterDirective();
    expect(directive).toBeTruthy();
  });
});
