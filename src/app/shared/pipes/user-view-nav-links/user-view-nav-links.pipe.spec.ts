import { UserViewNavLinksPipe } from './user-view-nav-links.pipe';

describe('UserViewNavLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new UserViewNavLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
