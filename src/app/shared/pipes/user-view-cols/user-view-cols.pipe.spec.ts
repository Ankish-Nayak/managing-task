import { UserViewColsPipe } from './user-view-cols.pipe';

describe('UserViewColsPipe', () => {
  it('create an instance', () => {
    const pipe = new UserViewColsPipe();
    expect(pipe).toBeTruthy();
  });
});
