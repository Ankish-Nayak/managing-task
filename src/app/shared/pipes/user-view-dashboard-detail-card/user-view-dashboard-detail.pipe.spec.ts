import { UserViewDashboardDetailPipe } from './user-view-dashboard-detail.pipe';

describe('UserViewDashboardDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new UserViewDashboardDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
